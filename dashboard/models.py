from django.db import models

# The dashboard app doesn't need any models, as it will use models from accounts and lending apps
# It will primarily consist of views that aggregate data from multiple models
